import React from 'react';
import { Grid } from '@material-ui/core';

const Filter = () => {
    return (
        <Grid container>
            <p> Filter by </p>
        </Grid>
    );
};

export default Filter;
